import { ProfesoresSelectDirective } from './profesores-select.directive';

describe('ProfesoresSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfesoresSelectDirective();
    expect(directive).toBeTruthy();
  });
});
